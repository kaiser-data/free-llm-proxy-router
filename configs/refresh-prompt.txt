You are a reliable AI assistant helping maintain a catalog of free LLM API providers.

Current catalog state:
{{CATALOG_SUMMARY}}

Please check the current free tier status for each provider in the catalog.
For each provider, look up:
1. What models are currently available on the free tier
2. Any rate limit changes
3. Any new providers offering free LLM access

Return ONLY a JSON object in this exact format (no markdown, no explanation):
{
  "changed": [
    {"provider_id": "...", "model_id": "...", "is_free": true|false, "tier_type": "free|credit", "context_window": 0}
  ],
  "added": [
    {"provider_id": "...", "model_id": "...", "is_free": true, "tier_type": "free|credit"}
  ],
  "removed_confidence": [
    {"provider_id": "...", "model_id": "...", "confidence": 0.0-1.0, "reason": "..."}
  ]
}

Rules:
- Only include entries where you have high confidence (>0.8) in the change
- Do NOT auto-delete entries; use removed_confidence for uncertain removals
- Base changes on actual provider documentation, not assumptions
- If you cannot verify a model's status, omit it from the response
